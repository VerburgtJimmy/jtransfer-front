// Dutch landing page. Prerendered to real HTML like the other content pages,
// overriding the app-wide ssr=false. The upload flow itself stays on "/" until
// the app is localised.
export const ssr = true;
export const prerender = true;
